import {
  addFriendsSchema,
  postSchema,
  acceptSchema,
  blockSchema,
  updateProfileSchema,
} from "./schemas";

export const validationSchema = {
  addFriendsSchema,
  postSchema,
  acceptSchema,
  blockSchema,
  updateProfileSchema,
};
