import { addFriendsSchema, postSchema,acceptSchema,blockSchema,DeleteSchema } from "./schemas";

export const validationSchema = {
  addFriendsSchema,
  postSchema,
  acceptSchema,
  blockSchema,
  DeleteSchema
};
